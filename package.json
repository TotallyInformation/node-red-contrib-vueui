{
    "name": "node-red-contrib-vueui",
    "version": "0.0.2",
    "description": "A node for Node-RED to create a single page website using Vue.js - ALPHA only, not ready for use.",
    "keywords": [
        "node-red", "vue", "dashboard"
    ],
    "main": "none",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "contributors": [
        {"name": "Julian Knight"},
        {"name": "Dave Conway-Jones"}
    ],
    "license": "Apache-2.0",
    "repository": {
        "type": "git",
        "url": "https://github.com/node-red/node-red-contrib-vueui.git"
    },
    "node-red": {
        "version": ">=0.16.0",
        "nodes": {
            "vueui_template": "nodes/vueui.js"
        },
        "notyetnodes": {
            "ui_base": "nodes/ui_base.js",
            "ui_template": "nodes/ui_template.js"
        }
    },
    "dependencies": {
        "serve-static": "^1.10.0",
        "socket.io": "^1.3.7"
    },
    "devDependencies": {
    }
}
